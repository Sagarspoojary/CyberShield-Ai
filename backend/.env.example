PORT=5050
NODE_ENV=development

# Path to your Firebase service account JSON key file (e.g. ./config/serviceAccount.json)
# Leave blank to run the server in Mock Auth mode.
FIREBASE_SERVICE_ACCOUNT_PATH=

# Fallback JWT Secret for Mock Auth Mode verification
JWT_SECRET=hackathon_secret_key_12345
